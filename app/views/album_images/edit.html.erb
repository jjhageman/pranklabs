<% title "#{@prank.title} | Edit #{@album.title}" %>
<div id="title">
  <h2><%= @prank.title %><span class="grey">: Edit Image<span></h2>
  <p class="small-link"><%= link_to 'Back to prank', @prank %> | <%= link_to 'Back to album', prank_album_images_path %></p>
</div>
<%= error_messages_for :image %>
<div id="caption">
<%- form_for(@image, :url => prank_album_image_path(:prank_id => @prank), :html => { :class => 'tool'} ) do |f| -%>
	<div class="fltlftimg">
		<%= image_tag @image.public_filename(:thumb), :class => 'album-pic' %>
		<p class="edit-link"><%= link_to '[x] Delete Image', prank_album_image_path, :confirm => 'Are you sure you want to delete the image?', :method => :delete if album_owner_or_admin?(@album) %></p>
	</div>
	<p>
		<%= f.label :caption %><br />
		<%= f.text_area :caption, :cols => 36, :rows => 5 %>
	</p>
	<p><%= f.submit "Save" %></p>
<%- end -%>
</div>